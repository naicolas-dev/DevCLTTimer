<UserControl x:Class="DevCLT.WindowsApp.Views.OnboardingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:DevCLT.WindowsApp.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis" />
    </UserControl.Resources>

    <Grid Margin="{StaticResource WindowPadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{Binding StepNumberText}"
                       Style="{StaticResource LabelText}"
                       VerticalAlignment="Center" />

            <Button Style="{StaticResource TertiaryButton}"
                    Content="Pular"
                    Command="{Binding SkipCommand}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center" />
        </Grid>

        <!-- Content Card -->
        <Border Grid.Row="1"
                Style="{StaticResource Card}"
                VerticalAlignment="Center"
                MaxWidth="400"
                HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="{Binding Title}"
                           Style="{StaticResource HeaderText}"
                           TextAlignment="Center"
                           Margin="0,0,0,12"
                           TextWrapping="Wrap" />

                <TextBlock Text="{Binding Description}"
                           Style="{StaticResource BodyText}"
                           TextWrapping="Wrap"
                           TextAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Style="{StaticResource SecondaryButton}"
                    Content="Voltar"
                    Command="{Binding BackCommand}"
                    Visibility="{Binding IsFirstStep, Converter={StaticResource BoolVis}, ConverterParameter=Invert}" />

            <Button Grid.Column="2"
                    Style="{StaticResource PrimaryButton}"
                    Content="{Binding PrimaryButtonText}"
                    Command="{Binding NextCommand}"
                    MinWidth="120" />
        </Grid>
    </Grid>
</UserControl>
